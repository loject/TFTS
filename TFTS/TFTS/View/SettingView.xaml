<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Title="Настройки"
             mc:Ignorable="d"
             x:Class="TFTS.View.SettingView">
    <ContentPage.Content>
        <ScrollView VerticalOptions="Start" Padding="10">
            <!-- TODO: fix this layout -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Grid.Row="0" Text="Управление" FontSize="Title" />
                <Label Grid.Column="0" Grid.Row="1" Text="Завершение круга по свайпу" TextColor="Black" FontSize="Medium" />
                <Switch Grid.Column="1" Grid.Row="1" IsToggled="{Binding LapDoneBySwipe}"/>
                <Label Grid.Column="0" Grid.Row="2" Text="Первый круг всегда полный(если количество кругов не целое)" TextColor="Black" FontSize="Medium" />
                <Switch Grid.Column="1" Grid.Row="2" IsToggled="{Binding FirstLapAlwaysFull}"/>
            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>