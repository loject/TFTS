<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TFTS.MainPage">

    <StackLayout>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Button Text="Забег" Command="{Binding GoToRaceCommand}"/>
            <Button Text="Табата" Command="{Binding GoToTobataCommand}"/>
            <Button Text="История" Command="{Binding GoToHistoryCommand}"/>
            <Button Text="Настройки" Command="{Binding GoToSettingsCommand}"/>
        </StackLayout>
    </StackLayout>

</ContentPage>
